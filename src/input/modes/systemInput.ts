import type { InputMode } from '../types';

export const systemInputMode: InputMode = {
  id: 'system',
  label: 'System Input',
  description: 'Use the browser and operating system input behavior.',
};
