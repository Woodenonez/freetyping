import type { InputMode } from '../types';

export const englishDirectInputMode: InputMode = {
  id: 'en-direct',
  label: 'English Direct',
  language: 'en',
  description: 'Direct English typing without text transformation.',
};
