# V4 Implementation Steps

Status: complete.

1. Select Apache-2.0 `rime-pinyin-simp` as the real Pinyin dictionary source.
2. Record the upstream source URL and license attribution.
3. Add a dictionary build script that downloads and parses `pinyin_simp.dict.yaml`.
4. Normalize spaced Rime spellings into continuous Pinyin keys.
5. Generate a separate browser-loadable dictionary artifact.
6. Keep generated dictionary data out of the main JavaScript bundle.
7. Lazy-load the generated dictionary when Chinese Pinyin mode is active.
8. Keep the built-in phrase entries as fallback and phrase overrides.
9. Rebuild candidate indexes after external dictionary load.
10. Preserve frequency-ranked candidate ordering from dictionary weights.
11. Preserve V3 segmentation, fuzzy matching, pagination, and punctuation behavior.
12. Show a small loading state in the candidate bar while the large dictionary loads.
13. Do not add custom phrase dictionaries or learned local user dictionaries.
14. Keep Pinyin data static and shipped with the app for privacy and static-hosting simplicity.
15. Add unit tests for generated dictionary installation.
16. Add browser smoke coverage for lazy-loaded dictionary candidates.
17. Tighten the editor-to-virtual-panel layout spacing.
18. Update README with source, license, and local-only privacy notes.
19. Run unit tests.
20. Run browser smoke tests.
21. Run full release checks.
22. Run GitHub Pages base-path build.
