name: Deploy to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test

      - name: Build
        run: npm run build
        env:
          GITHUB_PAGES_BASE: /freetyping/

      - name: Publish deploy branch
        run: |
          rm -rf /tmp/freetyping-pages
          mkdir /tmp/freetyping-pages
          cp -a dist/. /tmp/freetyping-pages/
          touch /tmp/freetyping-pages/.nojekyll

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch --orphan deploy
          git rm -rf . || true
          git clean -fdx
          cp -a /tmp/freetyping-pages/. .
          git add -f .
          git diff --cached --quiet && exit 0
          git commit -m "Deploy GitHub Pages"
          git push origin deploy --force
